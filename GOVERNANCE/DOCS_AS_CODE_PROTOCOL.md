# Docs As Code Protocol

Governance and course contracts are versioned text artifacts. Use stable headings, relative repository paths, structured tables or YAML/JSON where appropriate, and valid JSON for machine state.

Changes must be reviewable as diffs, tied to a task, and checked for broken authority links, merge markers, sensitive content, and state inconsistency. Generated documents do not override SOT files.
